interface:
  display_name: "Monthly creator activity reconciliation"
  short_description: "Reconcile normalized monthly creator activity with existing records"
  default_prompt: "Use $live-agency-creator-monthly-activity-reconcile to compare monthly activity and apply only explicitly authorized changes."
